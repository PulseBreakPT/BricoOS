# =============================================================================
# Brico2 backend — requirements de PRODUÇÃO
# =============================================================================
# Apenas o que backend/server.py realmente importa em runtime.
# Diferenças vs backend/requirements.txt (imagem de dev do Emergent):
#   - REMOVIDO: boto3, pandas, numpy, stripe, huggingface, jq, typer, black,
#     isort, flake8, mypy, pytest*  (nunca importados pelo código runtime)
#   - emergentintegrations (IA) é instalado à parte no Dockerfile porque vem
#     de um index próprio do Emergent e é opcional (só usado com OPENAI_API_KEY).
# =============================================================================

fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
python-dotenv>=1.0.1

# Sanitização do HTML dos emails recebidos antes de mostrar na app
nh3==0.3.6

# Gmail OAuth + envio de emails
google-auth>=2.55.0
google-auth-oauthlib>=1.4.0
google-api-python-client>=2.190.0

# Conversor de orçamentos PDF (quote_pdf.py): leitura do PDF do fornecedor,
# geração do PDF de venda, e upload multipart (FastAPI exige python-multipart
# no arranque para as rotas com UploadFile).
pymupdf>=1.26
reportlab>=4.1
python-multipart>=0.0.9

# Leitura das folhas Excel dentro do Anexos_Edição.zip do Correio Semanal
# (anexos_edicao.py) — só a biblioteca de leitura, nunca pandas/numpy.
openpyxl>=3.1
